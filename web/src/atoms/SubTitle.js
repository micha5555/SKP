import React from "react";

import './css/subTitle.css';

const SubTitle = ({text}) => {
  return <div
    className="subTitle"
  >
    <span>text</span>
  </div>
}

export default SubTitle;